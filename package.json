{
  "name": "prueba",
  "version": "1.0.0",
  "description": "Sitio estático pre-compilado listo para Cloudflare y GitHub Pages",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('✅ Sitio estático pre-compilado listo para desplegar.')\"",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  }
}